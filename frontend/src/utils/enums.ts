export enum StatusEnum {
  IDLE,
  LOADING,
  SUCCEEDED,
  FAILED,
}

export enum FillerCardEnum {
  // INVALID = -1,
  BOTTOM_MESSAGE,
  NO_COMMENTS,
  SHOW_CHILDREN,
}

export enum BarColourEnum {
  RED,
  ORANGE,
  YELLOW,
  GREEN,
  TEAL,
  BLUE,
}
